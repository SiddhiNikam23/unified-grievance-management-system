import React from 'react';import { AlertTriangle, Link as LinkIcon, X } from 'lucide-react';const DuplicateWarning = ({ duplicateCheck, onClose, onLinkToExisting, onSubmitAnyway }) => {    if (!duplicateCheck || !duplicateCheck.isDuplicate) return null;    const { similarComplaints, confidence, recommendation } = duplicateCheck;    const getRecommendationColor = () => {        if (recommendation === 'DUPLICATE') return 'bg-red-100 border-red-500 text-red-900';        if (recommendation === 'SIMILAR') return 'bg-yellow-100 border-yellow-500 text-yellow-900';        return 'bg-blue-100 border-blue-500 text-blue-900';    };    const getRecommendationIcon = () => {        if (recommendation === 'DUPLICATE') return '🚫';        if (recommendation === 'SIMILAR') return '⚠️';        return 'ℹ️';    };    return (        <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4">            <div className="bg-white rounded-lg shadow-2xl max-w-3xl w-full max-h-[90vh] overflow-y-auto">                {}                <div className={`p-6 border-l-4 ${getRecommendationColor()}`}>                    <div className="flex items-start justify-between">                        <div className="flex items-center gap-3">                            <span className="text-4xl">{getRecommendationIcon()}</span>                            <div>                                <h2 className="text-2xl font-bold">                                    {recommendation === 'DUPLICATE' && 'Duplicate Complaint Detected'}                                    {recommendation === 'SIMILAR' && 'Similar Complaints Found'}                                    {recommendation === 'UNIQUE' && 'Related Complaints Found'}                                </h2>                                <p className="text-sm mt-1">                                    Confidence: {confidence}% | Found {similarComplaints.length} similar complaint(s)                                </p>                            </div>                        </div>                        <button                            onClick={onClose}                            className="text-gray-500 hover:text-gray-700"                        >                            <X size={24} />                        </button>                    </div>                </div>                {}                <div className="p-6 bg-gray-50">                    {recommendation === 'DUPLICATE' && (                        <p className="text-gray-700">                            <strong>Your complaint appears to be a duplicate of an existing complaint.</strong>                             {' '}To avoid redundancy and speed up resolution, we recommend linking your complaint to the existing one.                             You'll be notified when the original complaint is resolved.                        </p>                    )}                    {recommendation === 'SIMILAR' && (                        <p className="text-gray-700">                            <strong>We found similar complaints that might be related to your issue.</strong>                             {' '}You can link your complaint to an existing one or submit it as a new complaint if you believe it's different.                        </p>                    )}                </div>                {}                <div className="p-6">                    <h3 className="text-lg font-semibold mb-4">Similar Complaints:</h3>                    <div className="space-y-4">                        {similarComplaints.map((complaint, index) => (                            <div                                key={index}                                className="border border-gray-200 rounded-lg p-4 hover:shadow-md transition-shadow"                            >                                <div className="flex items-start justify-between mb-2">                                    <div>                                        <span className="font-semibold text-blue-600">                                            {complaint.grievanceCode}                                        </span>                                        <span className="ml-3 px-2 py-1 bg-gray-200 text-gray-700 text-xs rounded-full">                                            {complaint.similarityScore}% match                                        </span>                                    </div>                                    <span className={`px-3 py-1 rounded-full text-xs font-bold ${                                        complaint.currentStatus === 'Resolved' ? 'bg-green-500 text-white' :                                        complaint.currentStatus === 'Under Review' ? 'bg-yellow-500 text-white' :                                        'bg-blue-500 text-white'                                    }`}>                                        {complaint.currentStatus}                                    </span>                                </div>                                <p className="text-gray-700 text-sm mb-2">                                    <strong>Description:</strong> {complaint.description}                                </p>                                <p className="text-gray-600 text-xs mb-2">                                    <strong>Filed by:</strong> {complaint.complainantName} on{' '}                                    {new Date(complaint.createdAt).toLocaleDateString()}                                </p>                                <p className="text-gray-600 text-xs mb-3">                                    <strong>Why similar:</strong> {complaint.reason}                                </p>                                {complaint.location && (                                    <p className="text-gray-600 text-xs mb-3">                                        <strong>Location:</strong> {complaint.location.latitude?.toFixed(4)}, {complaint.location.longitude?.toFixed(4)}                                    </p>                                )}                                <button                                    onClick={() => onLinkToExisting(complaint.grievanceCode, complaint.similarityScore, complaint.reason)}                                    className="w-full mt-2 flex items-center justify-center gap-2 px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-semibold transition-colors"                                >                                    <LinkIcon size={16} />                                    Link to This Complaint                                </button>                            </div>                        ))}                    </div>                </div>                {}                <div className="p-6 bg-gray-50 border-t flex gap-3">                    <button                        onClick={onSubmitAnyway}                        className="flex-1 px-6 py-3 bg-gray-600 hover:bg-gray-700 text-white rounded-lg font-semibold transition-colors"                    >                        Submit as New Complaint Anyway                    </button>                    <button                        onClick={onClose}                        className="px-6 py-3 bg-white hover:bg-gray-100 text-gray-700 border border-gray-300 rounded-lg font-semibold transition-colors"                    >                        Cancel                    </button>                </div>            </div>        </div>    );};export default DuplicateWarning;